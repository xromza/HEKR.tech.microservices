server:
    port: 8080

spring:
    application:
        name: catalog-service
    datasource:
        url: ${DB_URL}?currentSchema=catalog-schema
        username: ${DB_USER}
        password: ${DB_PASS}
        driver-class-name: org.postgresql.Driver
    data:
        redis:
            host: ${REDIS_HOST}
            port: ${REDIS_PORT}
    jpa:
        hibernate:
            ddl-auto: validate
        show-sql: true
        properties:
            hibernate:
                default-schema: catalog-schema
    
    flyway:
        enabled: true
        default-schema: catalog-schema
        create-schemas: true
        locations: classpath:db/migration
    
    rabbitmq:
        host: ${RABBITMQ_HOST}
        port: ${RABBITMQ_PORT}
        username: ${RABBITMQ_USER}
        password: ${RABBITMQ_PASS}
        listener: 
            simple: 
                retry:
                    enabled: true
                    max-retries: 3

management:
    endpoints:
        web:
            exposure:
                include: health, info, metrics
    endpoint:
        health:
            show-details: always