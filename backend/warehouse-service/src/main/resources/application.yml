server:
    port: 8084

spring:
    application:
        name: warehouse-service
    
    datasource:
        url: jdbc:postgresql://localhost:5432/hekr_store_db?currentSchema=warehouse-schema
        username: hekr_admin
        password: hekr_password
        driver-class-name: org.postgresql.Driver

    jpa:
        hibernate:
            ddl-auto: validate
        show-sql: true
        properties:
            hibernate:
                default-schema: warehouse-schema
    
    flyway:
        enabled: true
        default-schema: warehouse-schema
        create-schemas: true
        locations: classpath:db/migration
    
    rabbitmq:
        host: localhost
        port: 5672
        username: guest
        password: guest
        listener: 
            simple: 
                retry:
                    enabled: true
                    max-retries: 3
    data:
        redis:
            host: localhost
            port: 6379
management:
    endpoints:
        web:
            exposure:
                include: health, info, metrics
    endpoint:
        health:
            show-details: always