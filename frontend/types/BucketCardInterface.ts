export interface BucketCardInterface {
    variantid: number,
    brand: string,
    title: string,
    quantity: number,
    appliedPrice: number,
    subTotal: number,
    availableStock: number,
    priceType: string,
    imageURL: string;
    size: string,
    color: string,
}