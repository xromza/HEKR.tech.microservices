export interface CartItemInterface {
    variantId: number,
    brand: string,
    title: string,
    quantity: number,
    appliedPrice: number,
    subtotal: number,
    availableStock: number,
    size: string,
    color: string,
    priceType: string,
    imageUrl: string
}